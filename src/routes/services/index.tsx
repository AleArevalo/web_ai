import { component$ } from '@builder.io/qwik';

export default component$(() => {
  return (
    <div class="section">
      <div class="container center">
        <h1>Services</h1>
      </div>
    </div>
  );
});